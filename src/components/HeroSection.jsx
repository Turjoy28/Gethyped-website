import HeroText from './HeroText';
import VideoStatCard from './VideoStatCard';

export default function HeroSection() {
  return (
    <section>
      <HeroText />
      {/* Video stat cards will go here */}
    </section>
  );
}
